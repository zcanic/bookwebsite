# Embedding Artificial Intelligence into ERP Software A Conceptual View on Business AI with Examples from SAP S4HANA (Siar Sarferaz) (z-library.sk, 1lib.sk, z-lib.sk) - 第105部分

**原始页码**: 313 - 315
**段落数量**: 12
**翻译工具**: Claude Code CLI

## 📖 翻译策略和上下文信息

- **使用的翻译策略**: 完整上下文
- **前文上下文页码**: 312-312 (1978 字符)
- **后文上下文页码**: 316-316 (829 字符)

*注：翻译时已使用"完整上下文"策略，参考前后文上下文以确保术语一致性和内容连贯性*

**术语说明**: 最后一句话原文被截断，已结合【后文上下文】补充完整语义，即关于ISLM如何通过描述字符串识别特定管道的说明。

---

environments available for implementing artificial intelligence on the SAP Business Technology Platform. Since the fundamental content and framework concepts are alike, we will illustrate one of them, namely, SAP Data Intelligence, as an example.

**【译文】** SAP业务技术平台（SAP BTP）上有多种可用于实施人工智能的开发环境。由于基本内容和框架概念相似，我们将以其中之一，即SAP数据智能（SAP Data Intelligence），为例进行说明。

---

In the following section, we will use a scenario that involves predicting the delay in product delivery to demonstrate the technical implementation steps. We will first describe the required developments in SAP Data Intelligence and then proceed with the ABAP side.

**【译文】** 在下一节中，我们将使用一个预测产品交付延迟的场景来演示技术实施步骤。我们将首先描述SAP Data Intelligence中所需的开发工作，然后继续进行ABAP端的开发。

---

20.3.1	  Required Development in SAP BTP

**【译文】** 20.3.1 SAP BTP中所需的开发

---

1.	 Connection Management
For the development of the introduced use case, Connection Management is required for setting up the connectivity to the SAP S/4HANA system in order to read the training data remotely.

**【译文】** 1. 连接管理（Connection Management）
为了开发本用例，需要使用连接管理来建立与SAP S/4HANA系统的连接，以便远程读取训练数据。

---

2.	 ML Scenario Manager
The ML Scenario Manager app is used to define all the required implementation artifacts.

**【译文】** 2. 机器学习场景管理器（ML Scenario Manager）
机器学习场景管理器应用用于定义所有所需的实施构件。

---

3.	 Modeler
The Modeler app is used to create the necessary pipelines.

**【译文】** 3. 建模器（Modeler）
建模器应用用于创建必要的管道。

---

1.	 Datasets
Datasets that are stored in SAP Data Intelligence and are used for the implementation of the use case, such as during data science exploration.

**【译文】** 1. 数据集（Datasets）
存储在SAP Data Intelligence中并用于该用例实施（如在数据科学探索阶段）的数据集。

---

2.	 Notebooks
SAP Data Intelligence integrates Jupyter Notebook to provide an exploration environment for data scientists. Thus, exploration and development of the use case are combined to increase efficiency.

**【译文】** 2. 笔记本（Notebooks）
SAP Data Intelligence集成了Jupyter Notebook，为数据科学家提供探索环境。因此，用例的探索和开发结合在一起，以提高效率。

---

3.	 Pipelines
A pipeline is a graphical way of programming training and inference logic for a use case.

**【译文】** 3. 管道（Pipelines）
管道是一种图形化的编程方式，用于为用例编写训练和推理逻辑。

---

4.	 URL
Each artificial intelligence scenario has as a unique identifier, which in the current version of SAP Data Intelligence is part of the URL. We’ll use this identifier later to bind the artificial intelligence scenario with the ABAP content.

**【译文】** 4. URL
每个AI场景都有一个唯一标识符，在当前版本的SAP Data Intelligence中，它是URL的一部分。稍后我们将使用此标识符将AI场景与ABAP内容绑定。

---

Pipelines can be created based on templates that already contain the main steps. For our use case, select Python Producer from the Template dropdown for the training pipeline and the Python Consumer template for the inference pipeline.

**【译文】** 我们可以基于已包含主要步骤的模板来创建管道。对于我们的用例，请从模板下拉列表中为训练管道选择Python Producer，为推理管道选择Python Consumer模板。

---

As of the time of writing, the ISLM framework expects only one training and one inference pipeline for a side-by-side artificial intelligence use case. To identity those

**【译文】** 截至撰写本文时，ISLM框架要求每个Side-by-Side人工智能用例只能有一个训练管道和一个推理管道。为了识别这些管道，ISLM假定描述中包含字符串 [ISLM_TRAINING_PIPELINE]。在未来的版本中，这可能会被明确的标志所取代。

---

