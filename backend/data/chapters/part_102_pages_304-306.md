# Embedding Artificial Intelligence into ERP Software A Conceptual View on Business AI with Examples from SAP S4HANA (Siar Sarferaz) (z-library.sk, 1lib.sk, z-lib.sk) - 第102部分

**原始页码**: 304 - 306
**段落数量**: 8
**翻译工具**: Claude Code CLI

## 📖 翻译策略和上下文信息

- **使用的翻译策略**: 完整上下文
- **前文上下文页码**: 303-303 (654 字符)
- **后文上下文页码**: 307-307 (1577 字符)

*注：翻译时已使用"完整上下文"策略，参考前后文上下文以确保术语一致性和内容连贯性*

**术语说明**: 1. PAL: Predictive Analysis Library (预测分析库)，SAP HANA 中的算法库。
2. APL: Automated Predictive Library (自动预测库)，用于自动化机器学习。
3. CDS View: Core Data Services View (核心数据服务视图)。
4. Model Consumption: 译为'模型应用'，指在业务流程或界面中调用和使用模型预测结果的过程。
5. 最后一项译文参考了后文上下文补全了截断的句子。

---

HT-800 are considered. The available filter conditions are derived from the structure of the underlying CDS view for model training and vary therefore for each use case.

**【译文】** ……HT-800 被纳入考量。可用的过滤条件源自用于模型训练的底层 CDS 视图结构，因此随每个应用场景而异。

---

1. Model Versions
Displays the different model versions that resulted from training runs.

**【译文】** 1. 模型版本
显示由训练运行产生的不同模型版本。

---

2. Activate, Deactivate, Retrain, and Delete
Models can be activated for productive usage, deactivated, retrained if the data environment changed, or deleted by clicking these buttons.

**【译文】** 2. 激活、停用、重训练和删除
点击这些按钮可以将模型激活以用于生产环境、停用模型、在数据环境发生变化时重新训练模型，或直接删除模型。

---

3. Status
This attribute indicates whether the model is activated for productive usage (Active status).

**【译文】** 3. 状态
此属性指示模型是否已激活以用于生产使用（即“激活”状态）。

---

Listing 20.1 Generated CDS View for Model Consumption

**【译文】** 清单 20.1 用于模型应用的生成 CDS 视图

---

@EndUserText.label: 'ISLM AutoGenerated View   Z_PREDICT_TAX_
AMOUNT_CDS01 '
@AbapCatalog.sqlViewName: 'Z_D7A03819460EAE' --ISLM Random 
generated SQL name
@AbapCatalog.dataMaintenance: #DISPLAY_ONLY
@ClientHandling.type: #CLIENT_DEPENDENT
@ClientHandling.algorithm: #SESSION_VARIABLE
@VDM.private: false
@VDM.viewType: #COMPOSITE
define view Z_PREDICT_TAX_AMOUNT_CDS01
 as select from Z_PREDICT_TAX_AMOUNT_TF01
        (  p_clnt: $session.client  )
    as a
  association [1..1] to I_AIVS_SALESORDERITEM as _CKE_toBase on 
a.SalesOrder = 
_CKE_toBase.SalesOrder and
a.SalesOrderItem = _CKE_toBase.SalesOrderItem
{key a.SalesOrder as SalesOrder,
key a.SalesOrderItem as SalesOrderItem,
    a.TaxAmount as TaxAmount,
    a.Rr_TaxAmount as Rr_TaxAmount,
    a.Bar_Rr_TaxAmount as Bar_Rr_TaxAmount,
    a.Quantile_Rr_TaxAmount_100 as Quantile_Rr_TaxAmount_100,
 /* Associations */
    @ObjectModel.text.element: null
    @Consumption.hidden: true
    _CKE_toBase as _CKE_toBase
}

**【译文】** @EndUserText.label: 'ISLM AutoGenerated View   Z_PREDICT_TAX_
AMOUNT_CDS01 '
@AbapCatalog.sqlViewName: 'Z_D7A03819460EAE' --ISLM 随机
生成的 SQL 名称
@AbapCatalog.dataMaintenance: #DISPLAY_ONLY
@ClientHandling.type: #CLIENT_DEPENDENT
@ClientHandling.algorithm: #SESSION_VARIABLE
@VDM.private: false
@VDM.viewType: #COMPOSITE
define view Z_PREDICT_TAX_AMOUNT_CDS01
 as select from Z_PREDICT_TAX_AMOUNT_TF01
        (  p_clnt: $session.client  )
    as a
  association [1..1] to I_AIVS_SALESORDERITEM as _CKE_toBase on 
a.SalesOrder = 
_CKE_toBase.SalesOrder and
a.SalesOrderItem = _CKE_toBase.SalesOrderItem
{key a.SalesOrder as SalesOrder,
key a.SalesOrderItem as SalesOrderItem,
    a.TaxAmount as TaxAmount,
    a.Rr_TaxAmount as Rr_TaxAmount,
    a.Bar_Rr_TaxAmount as Bar_Rr_TaxAmount,
    a.Quantile_Rr_TaxAmount_100 as Quantile_Rr_TaxAmount_100,
 /* Associations */
    @ObjectModel.text.element: null
    @Consumption.hidden: true
    _CKE_toBase as _CKE_toBase
}

---

20.2.2 Coded Approach Based on PAL

**【译文】** 20.2.2 基于 PAL 的编码方法

---

PAL offers more than 100 algorithms related to artificial intelligence and is utilized when APL falls short in addressing the needs of embedded artificial intelligence application. This might occur, for instance, when the necessary algorithm is not

**【译文】** PAL 提供了 100 多种与人工智能相关的算法，通常在 APL 无法满足嵌入式人工智能应用需求时使用。这种情况可能发生在，例如，当 APL 的功能中未包含所需的算法，或者 APL 的自动化数据科学流程无法产生足够准确的预测结果时。

---

