# Embedding Artificial Intelligence into ERP Software A Conceptual View on Business AI with Examples from SAP S4HANA (Siar Sarferaz) (z-library.sk, 1lib.sk, z-lib.sk) - 第63部分

**原始页码**: 187 - 189
**段落数量**: 11
**翻译工具**: Claude Code CLI

## 📖 翻译策略和上下文信息

- **使用的翻译策略**: 完整上下文
- **前文上下文页码**: 186-186 (1128 字符)
- **后文上下文页码**: 190-190 (61 字符)

*注：翻译时已使用"完整上下文"策略，参考前后文上下文以确保术语一致性和内容连贯性*

**术语说明**: 1. Data Subject (数据主体) 和 Data Controller (数据控制者) 为 GDPR 法律术语。2. SoldToParty (售达方) 和 SalesOrganization (销售组织) 为 ERP/SAP 系统标准术语。3. Information Lifecycle Management 译为'信息生命周期管理'。4. Facade 译为'门面'，这是软件设计模式中的标准术语。

---

An authorization framework governs both blocking methods, ensuring that only authorized individuals, such as auditors, can access the blocked data. Following the conclusion of the retention period, both transactional and central master data must be erased.

**【译文】** 授权框架管理着这两种冻结方法，确保只有获得授权的人员（如审计员）才能访问被冻结的数据。在保留期结束后，交易数据和中央主数据都必须被清除。

---

10.2 Consent management harmonizes consent operations and data structures throughout various applications and products. This serves as the foundation for consent scenarios that extend beyond the upkeep of consent information within a single application or system.

**【译文】** 10.2 同意管理（Consent Management）统一了各个应用程序和产品中的同意操作与数据结构。这为那些超越单一应用或系统中同意信息维护的同意场景奠定了基础。

---

A local API, functioning as a consent facade or proxy, enables access to consent data. Generic user interfaces are available for searching and displaying this data. Moreover, this application lays the groundwork for centralizing consent data on an AI technology platform, where front-office applications gather consent documents and back-office applications utilize them for further processing.

**【译文】** 一个作为同意门面（facade）或代理的本地 API 实现了对同意数据的访问。系统提供了通用的用户界面用于搜索和显示这些数据。此外，该应用为在 AI 技术平台上集中管理同意数据打下了基础，即前台应用收集同意文档，后台应用利用这些文档进行进一步处理。

---

This establishes the framework for engaging with third parties to exchange not only personal data but also associated consent details. Furthermore, it offers transparency and evidence of the specific personal data and processes or purposes for which consent was granted.

**【译文】** 这建立了一个框架，用于与第三方互动，不仅交换个人数据，还交换相关的同意详情。此外，它还提供了透明度，并为获得同意的具体个人数据、流程或目的提供证据。

---

10.2.2 Embedded Artificial intelligence

**【译文】** 10.2.2 嵌入式人工智能

---

...handling concerning GDPR compliance. However, if the model retains personal information, periodic retraining is essential to remove blocked or erased records from the model. This retraining process can also be initiated automatically in response to blocking and deletion events generated by Information Lifecycle Management.

**【译文】** ……关于 GDPR 合规性的处理。然而，如果模型保留了个人信息，则必须定期进行重新训练，以便从模型中移除被冻结或擦除的记录。这一重训过程也可以响应信息生命周期管理（Information Lifecycle Management）生成的冻结和删除事件而自动启动。

---

• An attribute that holds the data subject and type of consent. For instance, if I_SALESORDER.SoldToParty represents a customer, then DataSubjectId = SoldToParty and DataSubjectIdType = 3 signify the data subject and type.

**【译文】** • 一个保存数据主体（data subject）和同意类型的属性。例如，如果 `I_SALESORDER.SoldToParty`（售达方）代表客户，则 `DataSubjectId = SoldToParty` 和 `DataSubjectIdType = 3` 表示数据主体和类型。

---

• An attribute that contains the data controller and type of consent. For example, if I_SALESORDER.SalesOrganization is a sales organization code, then ControllerName = SalesOrganization and ControllerType = 2 indicate the controller and type.

**【译文】** • 一个包含数据控制者（data controller）和同意类型的属性。例如，如果 `I_SALESORDER.SalesOrganization` 是销售组织代码，则 `ControllerName = SalesOrganization` 和 `ControllerType = 2` 指示控制者和类型。

---

To align with the intended consent purpose, the following characteristics must be ensured:

**【译文】** 为了与预期的同意目的（purpose）保持一致，必须确保以下特征：

---

• Application-specific functional attributes should represent the purpose. For instance, “I agree that the ERP vendor can use order data from the past four years to create a profile of my product preferences.” In this case, the term order data corresponds only to orders, so purpose X is defined by I_SALESORDER.SalesOrderType = 'OR' (OR = ORDER).

**【译文】** • 应用程序特定的功能属性应代表该目的。例如，“我同意 ERP 供应商可以使用过去四年的订单数据来创建我的产品偏好档案。”在这种情况下，术语“订单数据”仅对应于订单，因此目的 X 由 `I_SALESORDER.SalesOrderType = 'OR'` 定义（OR = 订单）。

---

• Consent-specific functional attributes should represent the purpose. For example, “I agree that the ERP vendor can use payment data from the past quarter to calculate a risk profile of my payment behavior.” The terms profile/profiling and product preferences must be represented by attributes of the purpose: Purpose.Action = 'Profiling' and Purpose.Aspect = 'Payment'.

**【译文】** • 同意特定的功能属性应代表该目的。例如，“我同意 ERP 供应商可以使用过去一个季度的支付数据来计算我的支付行为风险档案。”术语“档案/画像（profiling）”和“产品偏好”（此处指原文中的支付行为相关属性）必须由目的的属性来表示：`Purpose.Action = 'Profiling'` 和 `Purpose.Aspect = 'Payment'`。

---

