# Embedding Artificial Intelligence into ERP Software A Conceptual View on Business AI with Examples from SAP S4HANA (Siar Sarferaz) (z-library.sk, 1lib.sk, z-lib.sk) - 第108部分

**原始页码**: 322 - 324
**段落数量**: 14
**翻译工具**: Claude Code CLI

## 📖 翻译策略和上下文信息

- **使用的翻译策略**: 完整上下文
- **前文上下文页码**: 321-321 (621 字符)
- **后文上下文页码**: 325-325 (1541 字符)

*注：翻译时已使用"完整上下文"策略，参考前后文上下文以确保术语一致性和内容连贯性*

**术语说明**: 1. Side-by-side artificial intelligence: 译为'并行人工智能'或'并行AI'，指在SAP BTP等平台上独立于S/4HANA核心运行的AI服务模式，对应'Embedded'（嵌入式）。
2. Artifacts: 译为'工件'，指开发过程中产生的对象或文件。
3. Consume: 在API上下文中译为'消费'，指调用并使用服务返回的数据。
4. Scope item: 译为'范围项'，SAP术语，指预定义的业务流程范围。
5. 'metadata.'：原文段落首句'metadata.'为上一页被截断句子的结尾，译为'……元数据。'以体现语境延续。

---

Text to describe the artificial intelligence key question to be solved by the defined intelligent scenario.

**【译文】** 用于描述该已定义的智能场景所要解决的人工智能关键问题的文本。

---

3. Intelligent Scenario Type
Type of the intelligent scenario, which for our side-by-side artificial intelligence use case is SAP Data Intelligence.

**【译文】** 3. 智能场景类型
智能场景的类型，对于我们的并行（side-by-side）人工智能用例而言，此处为 SAP Data Intelligence。

---

4. Package
Package in which to store the development artifacts.

**【译文】** 4. 包
用于存储开发工件的包。

---

5. Prediction Class

**【译文】** 5. 预测类

---

6. External ID

**【译文】** 6. 外部 ID

---

An intelligent scenario can be saved as draft if additional changes are expected; otherwise, it’s published for use.

**【译文】** 如果预期还有后续更改，可以将智能场景保存为草稿；否则，将其发布以供使用。

---

metadata. These metadata methods can take various forms, such as the name of the CDS view used for data extraction or the ID of the business feature used to activate a scope item.

**【译文】** ……元数据。这些元数据方法可以采用多种形式，例如用于数据提取的 CDS 视图名称，或用于激活范围项（scope item）的业务功能 ID。

---

The ID of the scenario artifact related to artificial intelligence, which was developed in SAP Data Intelligence (as shown in Fig. 20.19), is stored as a characteristic of the class and is revealed through the method displayed in Listing 20.6.

**【译文】** 在 SAP Data Intelligence 中开发的与人工智能相关的场景工件 ID（如图 20.19 所示）被存储为该类的一个特征，并通过清单 20.6 中显示的方法公开。

---

Listing 20.6 Coding for GET_SCENARIO_GUID Method

**【译文】** 清单 20.6 GET_SCENARIO_GUID 方法的代码

---

METHOD IF_ISLM_INTELLIGENT_SCENARIO~GET_SCENARIO_GUID.
    "ISLM Specific: Retuen the ML scenario GUID via IF_ISLM_
INTELLIGENT_SCENARIO interface implementation
    rv_scenario_guid = me->mv_scenario_guid.
ENDMETHOD.

**【译文】** METHOD IF_ISLM_INTELLIGENT_SCENARIO~GET_SCENARIO_GUID.
    "ISLM Specific: Retuen the ML scenario GUID via IF_ISLM_
INTELLIGENT_SCENARIO interface implementation
    rv_scenario_guid = me->mv_scenario_guid.
ENDMETHOD.

---

With the ID, the ABAP and SAP Data Intelligence content are correlated and processed accordingly by the ISLM framework. The method shown in Listing 20.7 implements the API for consuming the inference results using the REST protocol.

**【译文】** 有了这个 ID，ABAP 和 SAP Data Intelligence 的内容就能相互关联，并由 ISLM 框架进行相应的处理。清单 20.7 中显示的方法实现了使用 REST 协议消费（consume）推理结果的 API。

---

Listing 20.7 Coding for CONSUME_INFERENCE Method

**【译文】** 清单 20.7 CONSUME_INFERENCE 方法的代码

---

METHOD CONSUME_INFERENCE.
DATA:
  lv_json_request_data  TYPE string,
  lr_islm_inference_provider TYPE REF TO 
cl_islm_inference_provider,
  lr_islm_inference_client TYPE REF TO 
cl_islm_inference_http_client.
* Initialise inference provider from ISLM
  lr_islm_inference_provider = new cl_islm_inference_
provider( me ).
  lr_islm_inference_provider->get_inference_client(
                                      IMPORTING eo_http_client = 
lr_islm_inference_client
                        es_bapireturn  = DATA(ls_bapiret) ).
* Check inference client is avlaible    IF lr_islm_inference_
client IS NOT INITIAL.
    lv_json_request_data ='{ "quanity":12 , "product_code":125}'.
    lr_islm_inference_client->add_request_body(
    EXPORTING  iv_header_payload = lv_json_request_data).
    lr_islm_inference_client->send_and_receive(
        IMPORTING
        ev_http_return_reason = DATA(lv_http_return_reason)
        ev_response_data      = DATA(lv_json_response_data)
        es_bapireturn         = DATA(ls_bapireturn)
        ev_http_return_code   = DATA(lv_http_return_code)
       ).
    lr_islm_inference_client->close( ).
  ENDIF.
ENDMETHOD.

**【译文】** METHOD CONSUME_INFERENCE.
DATA:
  lv_json_request_data  TYPE string,
  lr_islm_inference_provider TYPE REF TO 
cl_islm_inference_provider,
  lr_islm_inference_client TYPE REF TO 
cl_islm_inference_http_client.
* Initialise inference provider from ISLM
  lr_islm_inference_provider = new cl_islm_inference_
provider( me ).
  lr_islm_inference_provider->get_inference_client(
                                      IMPORTING eo_http_client = 
lr_islm_inference_client
                        es_bapireturn  = DATA(ls_bapiret) ).
* Check inference client is avlaible    IF lr_islm_inference_
client IS NOT INITIAL.
    lv_json_request_data ='{ "quanity":12 , "product_code":125}'.
    lr_islm_inference_client->add_request_body(
    EXPORTING  iv_header_payload = lv_json_request_data).
    lr_islm_inference_client->send_and_receive(
        IMPORTING
        ev_http_return_reason = DATA(lv_http_return_reason)
        ev_response_data      = DATA(lv_json_response_data)
        es_bapireturn         = DATA(ls_bapireturn)
        ev_http_return_code   = DATA(lv_http_return_code)
       ).
    lr_islm_inference_client->close( ).
  ENDIF.
ENDMETHOD.

---

With this, the implementation of our side-by-side artificial intelligence case is completed. The consumption API of the ABAP class provides the inference results, which can be integrated into business processes and user interfaces.

**【译文】** 至此，我们的并行人工智能案例的实施就完成了。该 ABAP 类的消费 API 提供了推理结果，这些结果可以集成到业务流程和用户界面中。

---

