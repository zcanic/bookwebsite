# Embedding Artificial Intelligence into ERP Software A Conceptual View on Business AI with Examples from SAP S4HANA (Siar Sarferaz) (z-library.sk, 1lib.sk, z-lib.sk) - 第101部分

**原始页码**: 301 - 303
**段落数量**: 17
**翻译工具**: Claude Code CLI

## 📖 翻译策略和上下文信息

- **使用的翻译策略**: 完整上下文
- **前文上下文页码**: 300-300 (702 字符)
- **后文上下文页码**: 304-304 (527 字符)

*注：翻译时已使用"完整上下文"策略，参考前后文上下文以确保术语一致性和内容连贯性*

**术语说明**: 1. Consumption Signature/API 译为“消费签名/消费API”，指供下游应用调用的接口定义。2. Inference API 译为“推理API”，指模型用于进行预测的接口。3. 原文第20.6节第5点(Publish/Save Draft)的描述文字似乎有误（重复了第4点的内容），此处按照原文直译。

---

SAP HANA artificial intelligence library to be used for the intelligent scenario (APL, PAL).

**【译文】** 用于智能场景的SAP HANA人工智能库（APL、PAL）。

---

1. General
Includes the Name, Description, and Package of the intelligent scenario.

**【译文】** 1. 常规（General）
包括智能场景的名称、描述和包。

---

2. Training Dataset
CDS view to read and train the artificial intelligence model. Data scientists recommend which application fields are required so that the application developer can define the CDS view accordingly.

**【译文】** 2. 训练数据集（Training Dataset）
用于读取和训练人工智能模型的CDS视图。数据科学家会建议需要哪些应用字段，以便应用开发人员能够相应地定义CDS视图。

---

3. Apply Dataset
CDS view to define the consumption signature for the artificial intelligence model.

**【译文】** 3. 应用数据集（Apply Dataset）
用于定义人工智能模型消费签名的CDS视图。

---

4. Target
Field of the training CDS view to be predicted.

**【译文】** 4. 目标（Target）
训练CDS视图中需要被预测的字段。

---

5. Keys
Key fields of the CDS view, which are determined automatically.

**【译文】** 5. 键（Keys）
CDS视图的关键字段，这些字段会自动确定。

---

6. Apply Output Configuration
The checkboxes here control the prediction output fields to be generated as part of the inference API. Score is selected by default and represents the regression prediction. Confidence stands for the error base value on the prediction. Percentile indicates the value below which a given percentage of observations in a group falls.

**【译文】** 6. 应用输出配置（Apply Output Configuration）
此处的复选框控制在推理API中生成的预测输出字段。“分数（Score）”默认被选中，代表回归预测值。“置信度（Confidence）”代表预测的误差基准值。“百分位数（Percentile）”表示一个数值，在给定的分组中，有一定百分比的观测值低于该数值。

---

1. Input
Displays the input for the intelligent scenario, which is basically the CDS view for model training.

**【译文】** 1. 输入（Input）
显示智能场景的输入，本质上就是用于模型训练的CDS视图。

---

2. Key role
Key fields of the CDS training view are displayed.

**【译文】** 2. 键角色（Key role）
显示CDS训练视图的关键字段。

---

3. Input role
Input fields of the CDS training view with which the underlying APL algorithm is trained are displayed.

**【译文】** 3. 输入角色（Input role）
显示CDS训练视图中用于训练底层APL算法的输入字段。

---

4. Target role
The target field of the CDS training view is displayed, which will be predicted based on training using the input fields.

**【译文】** 4. 目标角色（Target role）
显示CDS训练视图的目标字段，该字段将基于使用输入字段的训练来进行预测。

---

5. Publish/Save Draft
When we click one of these buttons, the target field of the CDS training view is displayed, which will be predicted based on training using the input fields.

**【译文】** 5. 发布/保存草稿（Publish/Save Draft）
当我们点击这些按钮之一时，会显示CDS训练视图的目标字段，该字段将基于使用输入字段的训练来进行预测。

---

1. Output
Displays the output for the intelligent scenario, which is basically the consumption API of the trained model.

**【译文】** 1. 输出（Output）
显示智能场景的输出，本质上就是已训练模型的消费API。

---

2. Key role
Key fields of the consumption API are displayed.

**【译文】** 2. 键角色（Key role）
显示消费API的关键字段。

---

3. Prediction role
Prediction fields of the consumption API with which the predicted results are exposed are displayed.

**【译文】** 3. 预测角色（Prediction role）
显示消费API中用于暴露预测结果的预测字段。

---

4. abap.applyclass
When we click the API Details button, we can view the generated consumption API as an ABAP method that can be integrated into business processes and user interfaces.

**【译文】** 4. abap.applyclass
点击“API详情（API Details）”按钮时，我们可以将生成的消费API查看为ABAP方法，该方法可集成到业务流程和用户界面中。

---

5. abap.applyview
When we click the API Details button, we can also view the generated consumption API as a CDS table function that can be integrated into business processes and user interfaces.

**【译文】** 5. abap.applyview
点击“API详情（API Details）”按钮时，我们还可以将生成的消费API查看为CDS表函数，该函数也可集成到业务流程和用户界面中。

---

