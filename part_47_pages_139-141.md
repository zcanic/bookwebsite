# Embedding Artificial Intelligence into ERP Software A Conceptual View on Business AI with Examples from SAP S4HANA (Siar Sarferaz) (z-library.sk, 1lib.sk, z-lib.sk) - 第47部分

**原始页码**: 139 - 141
**段落数量**: 5
**翻译工具**: Claude Code CLI

## 📖 翻译策略和上下文信息

- **使用的翻译策略**: 完整上下文
- **前文上下文页码**: 138-138 (2890 字符)
- **后文上下文页码**: 142-142 (2217 字符)

*注：翻译时已使用"完整上下文"策略，参考前后文上下文以确保术语一致性和内容连贯性*

**术语说明**: 1. Side-by-side artificial intelligence: 译为“侧并行人工智能”，指在ERP核心系统之外并行运行的AI服务，常用于SAP等ERP生态中描述扩展架构。 2. Embedded artificial intelligence: 译为“嵌入式人工智能”，指直接在ERP核心平台内运行的轻量级AI。 3. Annotations: 在ERP数据模型（如SAP CDS View）上下文中，译为“注解”，指赋予数据字段额外业务语义（如货币、预测能力）的元数据标记。

---

7 Solution Architecture
In this chapter, we define the solution architecture for embedding artificial intelligence into ERP software. The previously discussed ERP product qualities, ERP reference processes, ERP reference architecture, and ERP application patterns for artificial intelligence build the foundation for the solution architecture that we propose in this chapter. There are simple use cases like trending and predictions, which can be served with classic algorithms as regression and clustering. Usually, those algorithms also not require a lot of data, memory, or computing power. Therefore, we propose to implement this type of use cases within the ERP platform and call them embedded artificial intelligence. However, there are more complex user cases like image recognition or processing of natural language that need neuronal networks and deep learning. These algorithms demand high volume of data and system resources. To avoid that the critical business processes in ERP systems are negatively affected from excessive hardware consumption, we suggest scaling out this category of use cases to corresponding AI technology platform and called side-by-­side artificial intelligence. While we focus in this chapter on the overall architecture, we will discuss additional aspects (e.g., data protection, extensibility) in the next chapters.

**【译文】** 第7章 解决方案架构

在本章中，我们将定义把人工智能（AI）嵌入企业资源规划（ERP）软件的解决方案架构。此前讨论的ERP产品特性、ERP参考流程、ERP参考架构以及用于AI的ERP应用模式，构成了我们在本章提出的解决方案架构的基础。存在一些简单的用例，如趋势分析和预测，它们可以通过回归（regression）和聚类（clustering）等经典算法来实现。通常，这些算法不需要大量的数据、内存或算力。因此，我们建议在ERP平台内部实现此类用例，并将其称为“嵌入式人工智能（embedded artificial intelligence）”。然而，还有一些更复杂的用例，如图像识别或自然语言处理，它们需要神经网络和深度学习技术的支持。这些算法对数据量和系统资源有很高的要求。为了避免ERP系统中的关键业务流程因过度的硬件资源消耗而受到负面影响，我们建议将此类用例扩展到相应的AI技术平台上，这被称为“侧并行人工智能（side-by-side artificial intelligence）”。虽然本章主要关注整体架构，但在后续章节中，我们将讨论数据保护、可扩展性等其他方面的内容。

---

7.1 Guiding Principles
In conventional ERP systems, the fundamental data model for business entities, such as customers, products, and sales orders, is often repeatedly modeled for various objectives due to the need to accommodate minor domain-specific metadata. As a result, technology solutions for integration, user interfaces, analytics, and transactions each require their own distinct business object models, leading to an increased total cost of development (TCD) since the same information must be repeatedly provided. Furthermore, the presence of incompatible metamodels means that cross-functional issues like UI integration, extensibility, and authorization must be addressed multiple times, resulting in a high total cost of ownership (TCO) and hindering the adoption of solutions by customers. In contrast, modern ERP systems are built upon a single core data model that is repurposed for various contexts through domain-specific enhancements. For the suggested artificial intelligence architecture, we presume the existence of a modern ERP system with a unified core data model, which we will expand to accommodate the domain of artificial intelligence.

**【译文】** 7.1 指导原则

在传统的ERP系统中，为了适应细微的特定领域元数据需求，客户、产品和销售订单等业务实体的基础数据模型往往会针对不同的目标被重复建模。因此，用于集成、用户界面、分析和事务处理的技术解决方案都需要各自独立的业务对象模型，这导致了总开发成本（TCD）的增加，因为必须重复提供相同的信息。此外，不兼容元模型（metamodels）的存在意味着必须多次解决UI集成、可扩展性和权限管理等跨职能问题，从而导致高昂的总拥有成本（TCO），并阻碍客户采用这些解决方案。相比之下，现代ERP系统建立在单一核心数据模型之上，该模型可以通过特定领域的增强功能在各种情境中被复用。针对我们建议的AI架构，我们假定已经存在一个具有统一核心数据模型的现代ERP系统，我们将对该模型进行扩展，以适应人工智能领域的需求。

---

The data model of modern ERPs is built upon views that are established on top of database tables containing business data. Generally, ERP data models consist of thousands of cryptical database tables with intricate relationships. To make this data model accessible for business applications, views are created on top of these tables. These views transform the underlying structures and relationships into a core data model that is easier for humans to understand. For instance, sales orders are stored in multiple related tables, which are then encapsulated by a single view representing the sales order business object. As is common in the database domain, these views are defined using SQL statements that combine (e.g., JOIN, UNION) different tables and rename columns as needed. In the context of modern ERPs, this view technology is enhanced to include business semantics in the form of annotations. This allows domain-specific semantics to be added to the entire view or individual fields within the view. For example, the sales order field revenue could be enriched with a currency annotation, enabling ERP engines to automatically apply currency conversions. Similarly, the estimated revenue field could be enhanced with a prediction annotation, allowing the underlying artificial intelligence engine to forecast the value. This approach avoids redundant data modeling, reducing development costs and increasing comprehensibility. Additionally, cross-topics for artificial intelligence, such as extensibility and security, are addressed uniformly since they are all based on the same core data model.

**【译文】** 现代ERP的数据模型是建立在包含业务数据的数据库表之上的视图（views）构成的。通常，ERP数据模型包含数千个关系错综复杂的晦涩数据库表。为了让业务应用程序能够访问这些数据模型，需要在这些表之上创建视图。这些视图将底层结构和关系转换为人类更容易理解的核心数据模型。例如，销售订单存储在多个关联表中，这些表被封装在代表销售订单业务对象的单一视图中。正如数据库领域常见的那样，这些视图是使用SQL语句定义的，根据需要组合（如JOIN、UNION）不同的表并重命名列。在现代ERP的语境下，这种视图技术得到了增强，以注解（annotations）的形式包含了业务语义。这允许将特定领域的语义添加到整个视图或视图中的单个字段。例如，销售订单的“收入”字段可以加上货币注解，使ERP引擎能够自动应用货币转换。同样，“预估收入”字段可以加上预测注解，允许底层的人工智能引擎对该值进行预测。这种方法避免了冗余的数据建模，降低了开发成本并提高了可理解性。此外，由于都是基于同一个核心数据模型，人工智能相关的跨领域问题（如可扩展性和安全性）也能得到统一解决。

---

Modern ERP products cater to market demands for hybrid deployment models by offering consistent data models and compatible business processes. Typically, multiple deployment options are supported, including on-premise, public cloud, and private managed cloud. Private managed cloud refers to a cloud environment hosted entirely for one an organization, containing all relevant cloud components which are managed by the ERP vendor. From the customer’s perspective, private cloud is a deployment model where the cloud infrastructure is exclusively used by a single organization with multiple consumers (i.e., single tenant) and accessed via a virtual private network (VPN). It is owned, managed, and operated by the customer. In contrast, public cloud is a deployment model where the cloud infrastructure is shared among multiple customers and accessed via the Internet. The ERP vendor typically owns and manages the infrastructure and business software for public cloud. On-premise ERP software is installed and runs on computers at the customer’s location, rather than at a remote facility like a server farm or cloud on the Internet. The proposed artificial intelligence solution architecture is invariant across different deployment options and works in on-premise, privately managed, and public cloud environments.

**【译文】** 现代ERP产品通过提供一致的数据模型和兼容的业务流程，来满足市场对混合部署模式的需求。通常，系统支持多种部署选项，包括本地部署（on-premise）、公有云（public cloud）和私有托管云（private managed cloud）。私有托管云是指完全为一个组织托管的云环境，包含由ERP供应商管理的所有相关云组件。从客户的角度来看，私有云是一种部署模式，其云基础设施由拥有多个消费者的单一组织独享（即单租户），并通过虚拟专用网络（VPN）访问。它由客户拥有、管理和运营。相比之下，公有云是一种云基础设施在多个客户之间共享并通过互联网访问的部署模式。ERP供应商通常拥有并管理公有云的基础设施和业务软件。本地部署的ERP软件安装并运行在客户所在地的计算机上，而不是远程的服务器农场或互联网云端。我们提出的AI解决方案架构在不同的部署选项中保持不变，均适用于本地部署、私有托管和公有云环境。

---

In the context of artificial intelligence, a crucial principle is to bring algorithms to the ERP data rather than the other way around. Algorithms are typically composed of a few lines of code, are self-contained, and can be deployed flexibly. In contrast, application data is voluminous and contains numerous dependencies. As a result, replicating or extracting application data is often a complex and TCO-intensive task (e.g., handling deltas, meeting performance requirements, interpreting data semantically) and should be avoided. This consideration is taken into account in the suggested artificial intelligence solution architecture, which minimizes data transfer as much as possible. Specifically, the architecture pattern embedded artificial intelligence is defined, which adheres to the aforementioned golden rule as its key paradigm.

**【译文】** 在人工智能的语境下，一个至关重要的原则是“让算法向ERP数据靠拢”，而不是反其道而行之。算法通常由少量代码组成，是自包含的，且部署灵活。相比之下，应用数据体量庞大且包含无数的依赖关系。因此，复制或提取应用数据通常是一项复杂且极耗总拥有成本（TCO）的任务（例如，处理增量数据、满足性能要求、语义化解释数据），应尽量避免。建议的AI解决方案架构充分考虑了这一点，尽可能减少数据传输。具体而言，该架构定义了“嵌入式人工智能”模式，并将上述黄金法则作为其核心范式。

---

