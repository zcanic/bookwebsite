# Embedding Artificial Intelligence into ERP Software A Conceptual View on Business AI with Examples from SAP S4HANA (Siar Sarferaz) (z-library.sk, 1lib.sk, z-lib.sk) - 第103部分

**原始页码**: 307 - 309
**段落数量**: 8
**翻译工具**: Claude Code CLI

## 📖 翻译策略和上下文信息

- **使用的翻译策略**: 完整上下文
- **前文上下文页码**: 306-306 (1392 字符)
- **后文上下文页码**: 310-310 (1733 字符)

*注：翻译时已使用"完整上下文"策略，参考前后文上下文以确保术语一致性和内容连贯性*

**术语说明**: AMDP: ABAP Managed Database Procedures (ABAP托管数据库过程); Consumption: 在此上下文中指对已训练模型的使用或调用。

---

included in APL’s offerings or when APL’s automated data science processes do not yield sufficiently accurate predictions. PAL is the go-to solution for such excep­tional scenarios.

**【译文】** 包含在 APL 的功能库中，或者当 APL 的自动化数据科学流程无法产生足够准确的预测时。对于这类例外情况，PAL（预测分析库）是首选的解决方案。

---

In the case of PAL, the logic of artificial intelligence is crafted by the developer, rather than being automatically generated by the framework, as is the case with APL.

**【译文】** 在使用 PAL 的情况下，人工智能的逻辑是由开发人员构建的，而不是像 APL 那样由框架自动生成。

---

This approach has the benefit of giving the developer complete control, enabling them to fine-tune the implementation of the artificial intelligence logic to best suit the specific use case, following the guidance of data scientists.

**【译文】** 这种方法的优势在于赋予开发人员完全的控制权，使他们能够在数据科学家的指导下，对人工智能逻辑的实现进行微调，以最完美地契合特定用例。

---

The developer’s task is to implement an AMDP class, which adheres to predefined interfaces and offers methods such as training and consumption. These methods essentially embody the artificial intelligence logic and encompass aspects like pre­processing steps, data transformations, initiating the necessary PAL algorithms, and offering an API for the consumption of the trained model.

**【译文】** 开发人员的任务是实现一个 AMDP 类，该类需遵循预定义的接口，并提供训练（Training）和使用（Consumption）等方法。这些方法本质上体现了人工智能的逻辑，包含数据预处理、数据转换、调用必要的 PAL 算法，以及为已训练模型的使用提供 API 等环节。

---

In the following sec­tion, to keep the focus on the technical implementation, we will revisit a simple use case. Imagine being a cash specialist who classifies cash flows into various liquidity items, such as operations and investments. By leveraging artificial intel­ligence, we can obtain predictive insights into liquidity items by comparing actual and forecasted results. The actual liquidity items that are allocated can subsequently be replaced by the predicted liquidity items suggested by the artificial intelligence service.

**【译文】** 在下一节中，为了聚焦于技术实现，我们将重温一个简单的用例。试想你是一位资金专员，负责将现金流分类到不同的流动性项目中，例如运营资金和投资资金。通过利用人工智能，我们可以通过比对实际结果与预测结果，获得关于流动性项目的预测性洞察。随后，原本分配的实际流动性项目可以被人工智能服务建议的预测流动性项目所替代。

---

(refer back to Fig. 20.2). Next, we will create our intelligent scenario as depicted in Fig. 20.13. This time, we’ll select Classification as the Intelligent Scenario Type based off of our use case. Under Add Model, we must select PAL.

**【译文】** （请回顾图 20.2）。接下来，我们将创建如图 20.13 所示的智能场景。这一次，基于我们的用例，我们将选择“分类（Classification）”作为“智能场景类型（Intelligent Scenario Type）”。在“添加模型（Add Model）”下，我们必须选择 PAL。

---

The ABAP class must implement different interfaces, so the structure and behavior of those classes are standardized, which is a prerequisite for uniform han­dling by the ISLM framework.

**【译文】** ABAP 类必须实现不同的接口，从而使这些类的结构和行为实现标准化，这是 ISLM 框架对其进行统一处理的前提条件。

---

First, the method shown in Listing 20.2 provides metadata to the ISLM frame­work, such as the CDS view for model training or the API signature for consuming the model.

**【译文】** 首先，清单 20.2 中展示的方法向 ISLM 框架提供了元数据，例如用于模型训练的 CDS 视图或用于模型使用的 API 签名。

---

